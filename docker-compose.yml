version: "3.9"
services:
  api:
    build: ./apps/api
    ports: ["8000:8000"]
    env_file: ./apps/api/.env
    volumes: ["./apps/api:/app"]
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
